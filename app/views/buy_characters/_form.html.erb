<%= form_with(model: buy_character, local: true) do |form| %>
  <% if buy_character.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(buy_character.errors.count, "error") %> prohibited this buy_character from being saved:</h2>

      <ul>
      <% buy_character.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :image_sold %>
    <%= form.file_field :image_sold %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
